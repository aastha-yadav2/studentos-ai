import { useState } from "react"
import { AnimatePresence, motion } from "framer-motion"
import { ArrowRight, BrainCircuit, CalendarDays, CheckCircle2, Sparkles } from "lucide-react"
import { Button } from "@/components/ui/button"
const steps = [{ icon: Sparkles, title: "Your command center", text: "Live productivity, deadlines, and the one move that matters most." }, { icon: CalendarDays, title: "Plans that adapt", text: "StudentOS learns when you work best and reshapes future schedules." }, { icon: BrainCircuit, title: "Memory with purpose", text: "Your preferences, milestones, and AI reasoning stay connected." }, { icon: CheckCircle2, title: "Ready to explore", text: "You can now explore every part of this guided workspace." }]
export function OnboardingWalkthrough() { const [step, setStep] = useState(0); const [open, setOpen] = useState(true); if (!open) return null; const item = steps[step]; const Icon = item.icon; return <div className="fixed inset-x-4 bottom-5 z-50 mx-auto max-w-md"><AnimatePresence mode="wait"><motion.div key={item.title} initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 12 }} className="rounded-3xl border border-primary/30 bg-card/95 p-5 shadow-2xl backdrop-blur-xl"><div className="flex items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-2xl bg-primary/15 text-primary"><Icon className="size-5" /></span><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Demo tour · {step + 1}/{steps.length}</p><h2 className="mt-1 font-semibold">{item.title}</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">{item.text}</p></div></div><div className="mt-4 flex justify-between"><button onClick={() => setOpen(false)} className="text-sm text-muted-foreground hover:text-foreground">Explore freely</button><Button size="sm" onClick={() => step === steps.length - 1 ? setOpen(false) : setStep(step + 1)}>{step === steps.length - 1 ? "Start exploring" : "Next"}<ArrowRight className="size-3" /></Button></div></motion.div></AnimatePresence></div> }
