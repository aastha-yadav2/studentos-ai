import { PageShell } from "@/components/layout/page-shell"
export function OnboardingPage() { return <PageShell eyebrow="Setup" title="Onboarding" description="Collect student context before the Planner Agent creates the first personalized StudentOS execution plan." /> }
